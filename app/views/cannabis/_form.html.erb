<%= form_for @cannabi do |f| %>
    <%= f.text_field_tag ('strain') %>
    <br>
    <%= f.text_field :data, placeholder: "description" %>

    <br>
    <%= button_to "Create #{@cannabi.strain}", cannabi_path(@cannabi), method: :edit %>

<% end %>